[pytest]
testpaths = tests
# Permet aux tests d'importer agentic/ et nutrition/ depuis la racine du dépôt
# sans installer le projet ni bricoler sys.path dans chaque fichier.
pythonpath = .
addopts = -q --strict-markers
